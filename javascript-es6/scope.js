let puedesPasar = false;

if (true) {
    let puedesPasar = true;
}

console.log(puedesPasar);

const numeros = [1, 2, 3];

for (let i=0;i<numeros.length;i++) {
    const suma = 1 + numeros[i];
    console.log(suma);
}