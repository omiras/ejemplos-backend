let x = 1;

// objeto global que tiene un método de nombre log que muestra por el terminal el valor que pongas como parámetro (o los valores)
console.log(`El valor de la variable x es ${x}`);

// Otro objeto global que podemos usar en cualquier script
// Math es un objeto global que esta disponible en cualquier script
console.log(Math.random()); 